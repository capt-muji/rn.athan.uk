diff --git a/node_modules/expo-background-task/android/src/main/java/expo/modules/backgroundtask/BackgroundTaskConsumer.kt b/node_modules/expo-background-task/android/src/main/java/expo/modules/backgroundtask/BackgroundTaskConsumer.kt
index c8e51de..780d6af 100644
--- a/node_modules/expo-background-task/android/src/main/java/expo/modules/backgroundtask/BackgroundTaskConsumer.kt
+++ b/node_modules/expo-background-task/android/src/main/java/expo/modules/backgroundtask/BackgroundTaskConsumer.kt
@@ -37,7 +37,8 @@ class BackgroundTaskConsumer(context: Context?, taskManagerUtils: TaskManagerUti
     this.task = task
 
     val intervalMinutes = getIntervalMinutes()
-    BackgroundTaskScheduler.registerTask(context, intervalMinutes)
+    val requiresNetworkConnectivity = getRequiresNetworkConnectivity()
+    BackgroundTaskScheduler.registerTask(context, intervalMinutes, requiresNetworkConnectivity)
   }
 
   override fun didUnregister() {
@@ -51,4 +52,10 @@ class BackgroundTaskConsumer(context: Context?, taskManagerUtils: TaskManagerUti
     return (options?.get("minimumInterval") as? Number)?.toLong()
       ?: BackgroundTaskScheduler.DEFAULT_INTERVAL_MINUTES
   }
+
+  private fun getRequiresNetworkConnectivity(): Boolean {
+    val options = task?.options as? Map<String, Any?>
+    return (options?.get("requiresNetworkConnectivity") as? Boolean)
+      ?: BackgroundTaskScheduler.DEFAULT_REQUIRES_NETWORK_CONNECTIVITY
+  }
 }
diff --git a/node_modules/expo-background-task/android/src/main/java/expo/modules/backgroundtask/BackgroundTaskScheduler.kt b/node_modules/expo-background-task/android/src/main/java/expo/modules/backgroundtask/BackgroundTaskScheduler.kt
index 74c5719..2e6d7ba 100644
--- a/node_modules/expo-background-task/android/src/main/java/expo/modules/backgroundtask/BackgroundTaskScheduler.kt
+++ b/node_modules/expo-background-task/android/src/main/java/expo/modules/backgroundtask/BackgroundTaskScheduler.kt
@@ -28,6 +28,9 @@ object BackgroundTaskScheduler {
   // Default interval
   const val DEFAULT_INTERVAL_MINUTES = 60L * 24L // Once every day
 
+  // Default network requirement, kept true so existing callers are unaffected
+  const val DEFAULT_REQUIRES_NETWORK_CONNECTIVITY = true
+
   // Unique identifier (generated by us) to identify the worker
   private const val WORKER_IDENTIFIER = "EXPO_BACKGROUND_WORKER"
 
@@ -40,15 +43,23 @@ object BackgroundTaskScheduler {
   // Interval
   private var intervalMinutes: Long = DEFAULT_INTERVAL_MINUTES
 
+  // Whether the scheduled work requires a network connection
+  private var requiresNetworkConnectivity: Boolean = DEFAULT_REQUIRES_NETWORK_CONNECTIVITY
+
   // Tracks whether in foreground
   var inForeground: Boolean = false
 
   /**
    * Call when a task is registered
    */
-  fun registerTask(context: Context, intervalMinutes: Long) {
+  fun registerTask(
+    context: Context,
+    intervalMinutes: Long,
+    requiresNetworkConnectivity: Boolean = DEFAULT_REQUIRES_NETWORK_CONNECTIVITY
+  ) {
     numberOfRegisteredTasksOfThisType += 1
     this.intervalMinutes = intervalMinutes
+    this.requiresNetworkConnectivity = requiresNetworkConnectivity
 
     if (numberOfRegisteredTasksOfThisType == 1) {
       runBlocking {
@@ -57,6 +68,17 @@ object BackgroundTaskScheduler {
     }
   }
 
+  /**
+   * Builds the constraints applied to the worker. A task that does no networking can opt out
+   * of the connectivity requirement so the system may run it while the device is offline.
+   */
+  internal fun buildConstraints(requiresNetworkConnectivity: Boolean): Constraints =
+    Constraints.Builder()
+      .setRequiredNetworkType(
+        if (requiresNetworkConnectivity) NetworkType.CONNECTED else NetworkType.NOT_REQUIRED
+      )
+      .build()
+
   /**
    * Call when a task is unregistered
    */
@@ -104,9 +126,7 @@ object BackgroundTaskScheduler {
     val data = Data.Builder()
       .putString("appScopeKey", appScopeKey)
       .build()
-    val constraints = Constraints.Builder()
-      .setRequiredNetworkType(NetworkType.CONNECTED)
-      .build()
+    val constraints = buildConstraints(requiresNetworkConnectivity)
 
     // Get Work manager
     val workManager = WorkManager.getInstance(context)
diff --git a/node_modules/expo-background-task/build/BackgroundTask.types.d.ts b/node_modules/expo-background-task/build/BackgroundTask.types.d.ts
index 1c28333..901d90f 100644
--- a/node_modules/expo-background-task/build/BackgroundTask.types.d.ts
+++ b/node_modules/expo-background-task/build/BackgroundTask.types.d.ts
@@ -39,4 +39,12 @@ export type BackgroundTaskOptions = {
      *
      */
     minimumInterval?: number;
+    /**
+     * Whether the task requires network connectivity to run. Set it to `false` for tasks that do
+     * only local work, so the system can run them while the device is offline.
+     * - Defaults to `true`.
+     * - On Android this maps to the WorkManager `NetworkType.CONNECTED` constraint.
+     * - On iOS this maps to `BGProcessingTaskRequest.requiresNetworkConnectivity`.
+     */
+    requiresNetworkConnectivity?: boolean;
 };
diff --git a/node_modules/expo-background-task/expo-module.config.json b/node_modules/expo-background-task/expo-module.config.json
index 944b9e7..d4690a8 100644
--- a/node_modules/expo-background-task/expo-module.config.json
+++ b/node_modules/expo-background-task/expo-module.config.json
@@ -5,12 +5,6 @@
     "modules": ["BackgroundTaskModule"]
   },
   "android": {
-    "modules": ["expo.modules.backgroundtask.BackgroundTaskModule"],
-    "publication": {
-      "groupId": "host.exp.exponent",
-      "artifactId": "expo.modules.backgroundtask",
-      "version": "58.0.3",
-      "repository": "local-maven-repo"
-    }
+    "modules": ["expo.modules.backgroundtask.BackgroundTaskModule"]
   }
 }
diff --git a/node_modules/expo-background-task/ios/BackgroundTaskConstants.swift b/node_modules/expo-background-task/ios/BackgroundTaskConstants.swift
index 39478bb..4457d7e 100644
--- a/node_modules/expo-background-task/ios/BackgroundTaskConstants.swift
+++ b/node_modules/expo-background-task/ios/BackgroundTaskConstants.swift
@@ -6,6 +6,11 @@ public struct BackgroundTaskConstants {
   public static let EVENT_PERFORM_WORK = "onPerformWork"
   public static let EVENT_WORK_DONE = "onWorkDone"
 
+  /**
+   Default network requirement, kept true so existing callers are unaffected
+   */
+  public static let defaultRequiresNetworkConnectivity = true
+
   /**
    Startup argument that will cause us to simulate starting from the background
    */
diff --git a/node_modules/expo-background-task/ios/BackgroundTaskConsumer.swift b/node_modules/expo-background-task/ios/BackgroundTaskConsumer.swift
index c434189..02d2971 100644
--- a/node_modules/expo-background-task/ios/BackgroundTaskConsumer.swift
+++ b/node_modules/expo-background-task/ios/BackgroundTaskConsumer.swift
@@ -42,7 +42,10 @@ class BackgroundTaskConsumer: NSObject, EXTaskConsumerInterface {
       return
     }
 
-    BackgroundTaskScheduler.didRegisterTask(minutes: self.task?.options?["minimumInterval"] as? Int)
+    BackgroundTaskScheduler.didRegisterTask(
+      minutes: self.task?.options?["minimumInterval"] as? Int,
+      requiresNetworkConnectivity: self.task?.options?["requiresNetworkConnectivity"] as? Bool
+    )
   }
 
   func didUnregister() {
diff --git a/node_modules/expo-background-task/ios/BackgroundTaskScheduler.swift b/node_modules/expo-background-task/ios/BackgroundTaskScheduler.swift
index 906678b..712d728 100644
--- a/node_modules/expo-background-task/ios/BackgroundTaskScheduler.swift
+++ b/node_modules/expo-background-task/ios/BackgroundTaskScheduler.swift
@@ -62,11 +62,15 @@ public class BackgroundTaskScheduler {
     private var scheduler: BackgroundTaskScheduling = SystemBackgroundTaskScheduler()
     private var numberOfRegisteredTasksOfThisType: Int = 0
     private var intervalSeconds: TimeInterval = 12 * 60 * 60
+    private var requiresNetworkConnectivity: Bool = BackgroundTaskConstants.defaultRequiresNetworkConnectivity
 
-    func didRegisterTask(minutes: Int?) -> Bool {
+    func didRegisterTask(minutes: Int?, requiresNetworkConnectivity: Bool?) -> Bool {
       if let minutes = minutes {
         intervalSeconds = Double(minutes) * 60
       }
+      if let requiresNetworkConnectivity = requiresNetworkConnectivity {
+        self.requiresNetworkConnectivity = requiresNetworkConnectivity
+      }
       numberOfRegisteredTasksOfThisType += 1
 
       return numberOfRegisteredTasksOfThisType == 1
@@ -90,7 +94,7 @@ public class BackgroundTaskScheduler {
       stopWorkerOnce()
 
       let request = BGProcessingTaskRequest(identifier: BackgroundTaskConstants.BackgroundWorkerIdentifier)
-      request.requiresNetworkConnectivity = true
+      request.requiresNetworkConnectivity = requiresNetworkConnectivity
       request.requiresExternalPower = false
       request.earliestBeginDate = Date().addingTimeInterval(intervalSeconds)
 
@@ -129,6 +133,7 @@ public class BackgroundTaskScheduler {
       scheduler = SystemBackgroundTaskScheduler()
       numberOfRegisteredTasksOfThisType = registeredTaskCount
       intervalSeconds = 12 * 60 * 60
+      requiresNetworkConnectivity = BackgroundTaskConstants.defaultRequiresNetworkConnectivity
     }
 
     private func stopWorkerOnce() {
@@ -152,9 +157,13 @@ public class BackgroundTaskScheduler {
   /**
    * Call when a task is registered to keep track of how many background task consumers we have
    */
-  public static func didRegisterTask(minutes: Int?) {
+  public static func didRegisterTask(minutes: Int?, requiresNetworkConnectivity: Bool? = nil) {
     Task {
-      if await schedulerState.didRegisterTask(minutes: minutes) {
+      let isFirstTask = await schedulerState.didRegisterTask(
+        minutes: minutes,
+        requiresNetworkConnectivity: requiresNetworkConnectivity
+      )
+      if isFirstTask {
         try await tryScheduleWorker()
       }
     }
@@ -204,6 +213,16 @@ public class BackgroundTaskScheduler {
     await schedulerState.setSchedulerForTesting(scheduler)
   }
 
+  /**
+   Awaitable counterpart of `didRegisterTask`, which returns before its detached Task runs.
+   */
+  static func didRegisterTaskForTesting(minutes: Int?, requiresNetworkConnectivity: Bool?) async -> Bool {
+    return await schedulerState.didRegisterTask(
+      minutes: minutes,
+      requiresNetworkConnectivity: requiresNetworkConnectivity
+    )
+  }
+
   static func resetForTesting(registeredTaskCount: Int = 0) async {
     await schedulerState.resetForTesting(registeredTaskCount: registeredTaskCount)
   }
diff --git a/node_modules/expo-background-task/src/BackgroundTask.types.ts b/node_modules/expo-background-task/src/BackgroundTask.types.ts
index 24ff80b..ac44966 100644
--- a/node_modules/expo-background-task/src/BackgroundTask.types.ts
+++ b/node_modules/expo-background-task/src/BackgroundTask.types.ts
@@ -44,4 +44,12 @@ export type BackgroundTaskOptions = {
    *
    */
   minimumInterval?: number;
+  /**
+   * Whether the task requires network connectivity to run. Set it to `false` for tasks that do
+   * only local work, so the system can run them while the device is offline.
+   * - Defaults to `true`.
+   * - On Android this maps to the WorkManager `NetworkType.CONNECTED` constraint.
+   * - On iOS this maps to `BGProcessingTaskRequest.requiresNetworkConnectivity`.
+   */
+  requiresNetworkConnectivity?: boolean;
 };
