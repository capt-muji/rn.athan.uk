# Step 3: Nebula iteration and verdict landing (specified, branch-dependent)

0. **Anchor check:** section 3's counts for anchors `3-1`, `3-2`, `3-3`, `3-4` (all 1).
1. **Goal:** The owner steers one live iteration round on the dark home kinds' card and orbs; the verdict lands as code.
2. **Branch:** `git checkout -b feat/16a-nebula-verdict uat-2` BEFORE the landing only. The iteration itself runs on the simulator with Metro against the working tree; nothing commits until the verdict.
3. **Files:** iteration may touch `widgets/PrayerWidget.tsx` (the `DARK.card` literal at anchor `3-3`, the `NEBULA_*` constants at anchor `3-1`, the `OrbLight` specs at anchor `3-2`). The landing may additionally touch `shared/__tests__/widgetRenderer.test.ts`, `shared/__tests__/widgetContract.test.ts`, `scripts/generate-widget-assets.py`, `assets/widgets/*.png` (bake branch only), plus the three plan files.
4. **Tests first (red).** Written per verdict branch at landing time, in the branch's part 5 below. The iteration round itself runs no tests.
5. **Change.**

   **The iteration round (owner-steered, no commit):**
   - Prerequisites: simulator `EB00ED20-949A-4834-99A9-668F971EB53C` booted with the four dark home widgets placed (LOG §28 left them there; the owner confirms by looking), Metro running (restart with `yarn start` if dead), the Debug app installed on the sim (it is, from §28; if the owner says it is gone, STOP and ask before building anything).
   - Loop per candidate (LOG §18, ~35 seconds): edit the two literals (`DARK.card` and the `NEBULA_*` set or the `OrbLight` specs) in `widgets/PrayerWidget.tsx`; `xcrun simctl terminate <udid> com.mugtaba.athan || true`; `xcrun simctl launch <udid> com.mugtaba.athan`; poll the Metro log for the next `WIDGET: Standard timeline pushed`; `xcrun simctl terminate <udid> com.mugtaba.athan`; `pkill -f ExpoWidgetsTarget`; `sleep 12`; screenshot for the record with `xcrun simctl io <udid> screenshot ~/athan-device-sweep/16a-nebula-round/<n>.png` (the folder is created first; screenshots are records, nobody must read them); run the red-error pixel scan `python3 - <screenshot>` with a script that counts pixels matching the red-box colour and prints `RED PIXELS: <n>` (any `n > 0` is a finding). The 12-second wait is load-bearing: 5 seconds captures stale renders (LOG §18).
   - After each candidate the owner looks at the sim and answers. Continue, adjust, or verdict. After each round offer: continue, land the current look, or drop. If the owner wants a gradient wash: the `CARD_WASH` layer pattern is documented in LOG §16; re-add it as ten lines exactly as described there.
   - The round ends with the owner's verdict. Record it in `LOG.md` with the winning literals verbatim.

   **Verdict branch A: keep as runtime blur.** The orb machinery stays code; only literals may change.
   - Red first: if the winning design changes an orb COUNT, update the renderer nebula test (anchor `3-4`) to `<n> Circles on dark` and watch it fail against the pre-landing tree. If the count stays 3, the renderer test is unchanged. For the contract: add EVERY winning literal (the card value and every changed `NEBULA_*` colour) to the pinned-anchor list in `shared/__tests__/widgetContract.test.ts` (the block whose last three entries are `rgba(58, 118, 255, 0.35)`, `rgba(228, 74, 154, 0.40)`, `rgba(90, 58, 158, 0.25)`); against the old code the new `toContainEqual` anchors fail. If no literal changed at all, this branch is a docs-only commit (the verdict itself is the record).
   - Green: `npx jest shared/__tests__/widgetRenderer.test.ts shared/__tests__/widgetContract.test.ts --watchman=false --selectProjects=unit` passes; `npx tsc --noEmit` and `npx biome check . --error-on-warnings` exit 0.
   - Breaks (only when literals changed): one break per changed literal, `perl -pi -e "s/<WINNING_LITERAL>/<NEBULA_ERA_LITERAL>/" widgets/PrayerWidget.tsx` where `<NEBULA_ERA_LITERAL>` is the pre-landing value of the same slot (the card's is `rgba(18, 14, 40, 0.95)`); the expected failing test is the contract's pinned-literal assertion (or the renderer count test when the count changed). End `ALL AS EXPECTED: 1`.
   - Commit message: `<VERSION> - feat(16a): nebula verdict kept as runtime blur (<owner summary in five words>)`.

   **Verdict branch B: keep and bake PNGs.** The dark card and its orbs move into the PNG pipeline.
   - `scripts/generate-widget-assets.py` gains translucent iOS variants next to the opaque Android ones: two new entries `athan_widget_card_dark_small_translucent` / `..._medium_translucent` baked at the winning card colour with alpha preserved (RGBA paste over transparent, the `rounded_card` helper reused) with the winning orbs pre-blurred into the base (the script already bakes the identical geometry for Android; the iOS variants are the translucent forms, the file's own line-47 comment says so). Run `python3 scripts/generate-widget-assets.py` (writes `assets/widgets/`).
   - `widgets/PrayerWidget.tsx`: the iOS dark kinds draw the baked card as the base layer (an `Image` full-bleed under the content, the AImageEl pattern the Android path already uses) and `OrbLight` plus the `NEBULA_*` constants are deleted; light kinds and every other path unchanged; `containerBackground` on the dark kinds goes transparent (`rgba(0, 0, 0, 0)`, the lock precedent) because the PNG is the background.
   - Red first: the renderer nebula test (anchor `3-4`) is rewritten to `draws the baked dark card and no runtime orbs on dark kinds`: dark trees contain zero `Circle` markers and one `Image` whose source uri is the baked name; light trees unchanged. Against the pre-landing tree this fails (3 Circles present, no Image). The contract test's three nebula pinned literals are removed (their `toContainEqual` anchors fail against the landed tree if left; removing them before the change makes nothing red, so the red here is the renderer rewrite; the contract change rides the same commit).
   - Green: both suites pass; `shared/__tests__/widgetAssets.test.ts` passes with the two new PNGs pinned the way it pins the existing four; tsc, Biome exit 0.
   - Breaks: `perl -pi -e "s/<BAKED_URI_LITERAL>/athan_widget_card_dark_small/" widgets/PrayerWidget.tsx` expected to fail the renderer rewrite test; and delete one `assets/widgets/*.png` regeneration check per the assets test. End `ALL AS EXPECTED: 1`.
   - Commit message: `<VERSION> - feat(16a): nebula baked into the card PNGs (archive-cheap dark kinds)`.

   **Verdict branch C: drop the orbs.** The dark kinds go back to a flat card.
   - `widgets/PrayerWidget.tsx`: `OrbLight`, the `NEBULA_*` constants (anchor `3-1`), and the now-unused imports (`Circle`, `blur`, `scaleEffect` if unreferenced elsewhere) are deleted; `DARK.card` becomes the owner's chosen flat literal from the iteration (if the owner picked a flat colour they named; otherwise the pre-nebula committed `e4149396` value `rgba(53, 36, 137, 0.88)`; ask the owner which if the round ended without one).
   - Red first: the renderer nebula test (anchor `3-4`) becomes `draws no runtime orbs on any theme` asserting zero `Circle` markers on dark and light, both families; against the pre-landing tree it fails (3 on dark). In the same commit the contract's three nebula pinned literals are removed AND the chosen flat card literal is added to the pinned anchors.
   - Green, breaks: one break, `perl -pi -e "s/<CHOSEN_FLAT_LITERAL>/rgba(18, 14, 40, 0.95)/" widgets/PrayerWidget.tsx`, expected failing test: the contract's pinned-anchor assertion for the flat card (the layout no longer carries it). End `ALL AS EXPECTED: 1`.
   - Commit message: `<VERSION> - feat(16a): nebula dropped, dark kinds flat again (<owner words>)`.

   **Verdict branch D: defer.** The owner wants to think. `git checkout -- widgets/PrayerWidget.tsx`, a docs-only commit records the deferral, and the step is DONE with the committed study state standing.
6. **Green:** per branch above; always also `npx tsc --noEmit` and `npx biome check . --error-on-warnings` exit 0, and the full `yarn validate` inside the commit hook.
7. **Breaks:** per branch above; every break's search text is a literal this plan names (`<WINNING_CARD_LITERAL>`, `<BAKED_URI_LITERAL>`, `DARK.card`'s value), never incidental code.
8. **Version and commit.** The version command from step 2 (prints the next patch after `uat-2`). Files per branch. The commit message starts `<VERSION> - ` per branch.
9. **Review:** the executor's own diff review, recorded in `LOG.md`: only the named files changed; the verdict and its literals are recorded verbatim; no visual changed beyond the verdict's own scope.
10. **Merge:** `git checkout uat-2 && git merge --no-ff feat/16a-nebula-verdict -m "Merge feat/16a-nebula-verdict into uat-2: session 16a step 3, reviewed"`.
11. **Done when:** the verdict (or deferral) is in `LOG.md` with literals; the branch's checks pass; the checklist is ticked with the branch letter.

Device verification of the landed look is step 4's eyeball pass, not this step's; the sim iteration is the preview only.
