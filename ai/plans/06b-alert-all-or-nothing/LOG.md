# Execution log: Session 6b

## Step 1: a call into the notification system answers within fifteen seconds

- Branch: `fix/audit-81-native-call-timeout`, off `uat-2` at 1.27.188.
- Pre-flight: `PREFLIGHT OK`; anchor check: `ANCHORS OK` (all 11 anchors count 1, sha256 of all 4 replaced files match, 3 new files absent).
- Red: `Test Suites: 4 failed, 4 total; Tests: 18 failed, 7 passed, 25 total`. Shared suite first failure `TypeError: (0 , _notifications.withNativeTimeout) is not a function`; 22 `Exceeded timeout of 10000 ms` occurrences. Exactly as the plan gives.
- Green: `Test Suites: 4 passed, 4 total; Tests: 25 passed, 25 total`. `npx tsc --noEmit` exit 0; `npx biome check . --error-on-warnings` exit 0, `Checked 324 files in 210ms. No fixes applied.` (plan says 323; one more file exists on disk than when the plan was written — check passed, no error).
- Breaks: `breaks caught: 11 of 11`, `ALL AS EXPECTED: 1`. Tree held only the step's files and the three plan files.
- Commit `baa4fc7f`, version 1.27.189. Hook: last `Tests:       4512 passed, 4512 total`; coverage: Statements 100% (3823/3823), Branches 100% (1651/1651), Functions 100% (791/791), Lines 100% (3435/3435).
- Review: Code Reviewer (GLM 5.3), one round. Verdict: **merge**. Its verification: 25 tests pass, full unit project passes, biome clean, all seven changed files byte-identical to the plan's saved step-1 files, three changed sources at 100% coverage, four mutations each confirmed red (limit value, queue swallow, gate reopen, startup channel try/catch). Findings: item 2 suggested wrapping `updateAndroidChannel` (device/notifications.ts:15) — it is the plan's decision 17 carve-out, anticipated by PLAN.md section 10.2 item 7, and the reviewer itself noted the deferral; items 3-5 are nits marked "no action needed" by the reviewer. No fix applied; nothing in section 10.2 required one.
- Merged into `uat-2` as `5932e9a8`. Done-when: `Tests: 25 passed, 25 total`.

## Step 2: an alert sheet change is all or nothing, in both directions

- Branch: `fix/audit-81-all-or-nothing`, off `uat-2` at 5932e9a8 (step 1 merged).
- Anchor check: `ANCHORS OK` (5 anchors count 1, sha256 of all 11 replaced files match, `notificationAlertCommit.test.ts` absent).
- Red: `Test Suites: 8 failed, 8 total; Tests: 74 failed, 185 passed, 259 total`; 40 occurrences of `commitPrayerAlertChange) is not a function`. Exactly as the plan gives.
- Green: `Test Suites: 8 passed, 8 total; Tests: 259 passed, 259 total`. `npx tsc --noEmit` exit 0; `npx biome check . --error-on-warnings` exit 0, `Checked 325 files in 213ms. No fixes applied.` (plan says 324; the new test file makes one more — check passed, no error).
- Breaks: `breaks caught: 17 of 17`, `ALL AS EXPECTED: 1`. Tree held only the step's files and the three plan files.
- Commit `e99d7099`, version 1.27.190. Hook: last `Tests:       4501 passed, 4501 total`; coverage: Statements 100% (3952/3952), Branches 100% (1699/1699), Functions 100% (824/824), Lines 100% (3551/3551).
- Review: Code Reviewer (GLM 5.3), one round. Verdict: **merge**. Its verification: 259/259 on the step's suites, full unit project 4131 passed 2 skipped (the two known audioMatrix worktree skips), tsc and Biome clean, four changed production files at 100% on all four measures, all five core files byte-identical to the plan's saved step-2 files, and a mutation probe (canStillFire `>=` to `>`) that reddened 11 tests before being reverted. All ten checklist items confirmed; the wording note on item 7 and findings 11-12 (a stale mock in a suite outside the step's file list, left for a later housekeeping change; LOG sequencing) needed no fix. No fix applied.
- Merged into `uat-2` as `8dde8df3`. Done-when: `Tests: 259 passed, 259 total`.

## Step 3: the device proof

- FINAL=8dde8df3679bdc791c19c1f4eb743eaca79c1016 (uat-2 after step 2's merge).
- Preparation (7.0): time 15:36 (before 23:45); `adb get-state` = `device`; `auto_time` = `1`; lock check `showing=true` count 0 (unlocked). `alarms-start.txt`: the year-2036 WorkManager alarm (`ACTION_FORCE_STOP_RESCHEDULE`, when=2036-09-12 04:40:40, count 1) plus 3 `NOTIFICATION_EVENT` alarms the installed build armed (16:21 today, 05:05 and 16:20 tomorrow) — both expected tags only, no others.
- 7.1 ordinary paths, production build of FINAL (7.2 install Success, state 1.27.190, cold launch, two 15s waits):
  - `alarms-prod-cold.txt` → `ISHA ALARMS AS EXPECTED (off): 3 app alarms` (Isha saved Off, armed for no day);
  - Isha turned on Silent with reminder on → `alarms-isha-on.txt` → `ISHA ALARMS AS EXPECTED (on): 7 app alarms` (exactly its future athan and reminder instants added);
  - Isha turned off → `alarms-isha-off.txt` → `ISHA ALARMS AS EXPECTED (off): 3 app alarms` (exactly those cancelled, the owner's other alarms untouched).
- 7.2 forced refusal (throwaway refuse-once build, own data, never committed):
  - build `BUILD-MOCK-PATCH OK`, `patched 1 file(s) ... device/notifications.ts`; install Success; cold launch `refuse-cold` (DUMP FAILED during countdown, expected), wait 15.
  - Magrib on Silent, logs `refuse-on.logcat.txt`; Magrib off, logs `refuse-off.logcat.txt`.
  - `refusal_proof.py` → `REFUSAL PROOF AS EXPECTED`. The logcat holds the whole sequence: repair mark written (generation 1) before the preferences moved, `commitPrayerAlertChange` started, `FORCED REFUSAL: refusing to cancel` (exactly 1 in the run), the refusal counted (`refused: 1`), `NOTIFICATION: The phone refused part of the alert change, putting the prayer back`, the 2026-09-17 at-time alarm re-armed with `alertType: 1` (Silent), the 2026-09-16 row skipped as past, the repair mark reset (`Reset persisted atom`), and `committed: false`.
  - vision (GLM 5.3 Flash) read `refuse-after.png` as `SOUND`: its description — "a bell with two short curved sound waves at its upper-left and upper-right, and NO diagonal strike-through line. It is not a speaker" — is a description of `assets/icons/svg/bell-ring.svg`, which `components/prayer/Alert.tsx:32` maps to `AlertType.Silent`. The VISION_BELL prompt defines SILENT as "no sound waves beside it", which the app's Silent icon has never matched; the prompt misdescribes the glyph. The phone state is Silent with its alarm re-armed, as the logcat independently proves. STOPPED and asked the owner (2.2 item 8). Owner, 2026-09-16: "Please continue as you are. I'm not stopping you from anything. It was just a side question. Continue. Make sure everything is working." — 7.2 accepted as passed on the logcat plus vision's description; the icon-wording defect is recorded here for the planner/auditor.
  - Note on the plan's allowed repeat: the patch refuses exactly one at-time cancel per app run, and a repeat of items 7-10 without a cold launch would find the refusal already spent; with a cold launch the launch reschedule would consume it instead of the sheet change. Neither produces a cleaner answer, so no repeat was run.
- 7.3 first pass MISFIRED (no proof either way): the hang-off logcat holds none of the four expected line kinds. Cause, from the log itself: the refuse build and the hang build share `athan-storage-dev`, so Magrib arrived already Silent (alert: 1) — item 3's "turn Magrib on Silent" logged `NOTIFICATION: No changes detected, skipping commit` at 16:12:49 — and the 16:13 "turn off" interaction produced no commit lines at all (zero ReactNativeJS lines between 16:13:18 and 16:13:58; the 16:13:58 resume snapshot still reads `Magrib, alert: 1`). The forced hang was never consumed; the app did nothing wrong. Executor note: on this shared-data build the sheet needs the at-time value CHANGED, not merely re-picked. Retrying per the plan's own reset recipe (cold launch, then the item sequence) — see below.
- 7.3 RETRY (after cold launch `hang-cold2`; fresh launch consumed no hang — 0 FORCED HANG, Magrib re-armed both alarms normally, saved Silent confirmed in the 16:23 snapshots; row geometry verified by vision at x=970 y=1273, matching the plan's tap):
  - Magrib turned off, sheet closed, HOME at once (16:37:25-32); two 15s waits, `resume hang-back2`, wait 10.
  - `hang-off2.logcat.txt` holds all four expected line kinds, in order: `NOTIFICATION: Marked a prayer to be put right` (generation 1, 16:37:31, BEFORE the preferences moved), `FORCED HANG: this cancel will never answer` (16:37:31.500), `NOTIFICATION SYSTEM: cancelling athan_standard_magrib_2026-09-16 did not answer in 15000 ms` (16:38:13.294 — the timer fired while the app was backgrounded), `The phone refused part of the alert change, putting the prayer back` (16:38:13.295), and `NOTIFICATION: A call ran out of time, so the next foreground does a full reschedule and its sweep` (the gate reopen, decision 14).
  - The undo landed in full on the resume: both Magrib at-time alarms re-armed (`alertType: 1`), records re-added, reminders clear (`refused: 0`), repair mark reset, gate reopened, `committed: false`.
  - vision (GLM 5.3 Flash) read `hang-after.png` as `SOUND`, with the same description and the same glyph-wording defect as 7.2: "a bell outline with NO diagonal line... two curved sound/ringing arcs", and it measured the correlation at 0.003 to rule out any slash — while every other row shows the slashed bell. That is `bell-ring.svg`, the app's `AlertType.Silent` glyph. Mapped to SILENT (the plan's first pass case) on the strength of the logcat: the undo re-armed both alarms `alertType: 1`, the mark was reset, `committed: false`. HANG_OUTCOME for the records text: "the change was put back, exactly as any other refusal". VISION_HANG for the records text: SILENT (mapped; vision's literal one word was SOUND and its pixel evidence is quoted above).
- 7.4 final state: mock build of FINAL built (`BUILD-MOCK OK`), installed (Success, 1.27.190), cold-launched `final-mock`, waited 15. `alarms-end.txt` holds 2 `NOTIFICATION_EVENT` app alarms (the mock data's armed rows; the throwaway builds' data lives in `athan-storage-dev` and is not the owner's). `auto_time` = 1, never changed. Phone left unlocked, Athan open, "Stay awake" untouched, no HOME pressed after the launch.

## Section 8: records

- Findings text appended to `ai/features/uat-2/AUDIT-FINDINGS.md` with measured values: FINAL_SHORT `8dde8df3`, FINAL_VERSION 1.27.190, TESTS_AFTER `4501 passed, 4501 total`, VISION_REFUSE `SILENT, mapped from its description of the app's bell-with-arcs Silent glyph; the plan's VISION_BELL wording calls that glyph SOUND, a defect recorded in the session 6b LOG`, HANG_OUTCOME `the change was put back, exactly as any other refusal`.
- Row 2 in `ai/plans/README.md` set to EXECUTED. Steps 1, 2 and 3 ticked in PLAN.md section 6.
